package com.example.redisomdgs.datafetchers;

//@SpringBootTest(classes = {DgsAutoConfiguration.class, VehicleDataFetcher.class})
class VehicleDataFetcherTest {

//    @Autowired
//    DgsQueryExecutor dgsQueryExecutor;

    //    @Autowired
//    EntityStream entityStream;
//
//    @Test
//    void vehicles() {
////        SearchStream<Vehicle> stream = entityStream.of(Vehicle.class);
//
////        List<Vehicle> vehicleList = stream.filter(Vehicle$.MODEL_YEAR.eq(2012)).collect(Collectors.toList());
////        System.out.println(vehicleList.size());
//
//    }
}
